<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AlgoCripto"
        tools:targetApi="31">
        <activity
            android:name=".ChaumActivity"
            android:exported="false" />
        <activity
            android:name=".DiffieHellmanKeyProtocolActivity"
            android:exported="false" />
        <activity
            android:name=".ElGamalActivity"
            android:exported="false" />
        <activity
            android:name=".Rc4Activity"
            android:exported="false" />
        <activity
            android:name=".RsaActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AlgoCripto.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>